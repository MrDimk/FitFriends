{
  "name": "joint-training",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/joint-training/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/joint-training",
        "main": "apps/joint-training/src/main.ts",
        "tsConfig": "apps/joint-training/tsconfig.app.json",
        "assets": ["apps/joint-training/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/joint-training/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "joint-training:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "joint-training:build:development"
        },
        "production": {
          "buildTarget": "joint-training:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/joint-training/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/joint-training/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
